import React from 'react';
import './NextButton.css';
import {useRef} from 'react';

export const NextButton = ({handleAnswerOptionClick}) => {
    return (
        <></>
        // <button className='nextButton'  ref={handleAnswerOptionClick} disabled>Next level</button>
    )
}